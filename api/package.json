{
  "name": "@finilo/api-workspace",
  "version": "1.0.0",
  "description": "Finilo API monorepo workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build"
  },
  "keywords": [
    "finilo",
    "api",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "lerna": "^8.2.2",
    "typescript": "^5.8.3"
  }
}
